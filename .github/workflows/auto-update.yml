name: Auto Update Tweet Data

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests openpyxl pandas

      - name: Run update script
        run: python3 update_dashboard.py
        continue-on-error: true

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/daily_tweets.json data/dashboard_data.xlsx data/dashboard_data.json
          git diff --staged --quiet || git commit -m "🤖 Auto update $(date '+%Y-%m-%d %H:%M:%S')" && git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy to GitHub Pages
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git checkout gh-pages
          git checkout main -- dashboard.html data/daily_tweets.json data/dashboard_data.json
          git add .
          git diff --staged --quiet || git commit -m "📊 Update dashboard $(date '+%Y-%m-%d %H:%M:%S')" && git push
          git checkout main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Send Telegram notification
        run: |
          # 读取最新数据
          TODAY_COUNT=$(python3 -c "import json; d=json.load(open('data/dashboard_data.json')); print(d['today']['count'])")
          WEEK_AVG=$(python3 -c "import json; d=json.load(open('data/dashboard_data.json')); print(int(d['week_avg']))")
          MAX_COUNT=$(python3 -c "import json; d=json.load(open('data/dashboard_data.json')); print(d['stats']['max'])")
          LAST_UPDATE=$(python3 -c "import json; d=json.load(open('data/dashboard_data.json')); print(d['last_update'])")

          # 获取最近3天数据
          RECENT_DAYS=$(python3 << 'PYEOF'
import json
d = json.load(open('data/dashboard_data.json'))
for day in d['recent_days'][-3:]:
    print(f"{day['date']}: {day['count']}条")
PYEOF
)

          # 发送Telegram消息
          curl -s -X POST \
            "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "{
              \"chat_id\": \"${{ secrets.TELEGRAM_CHAT_ID }}\",
              \"text\": \"🤖 *Elon Musk 推文数据更新*\n\n📅 $(date '+%Y-%m-%d') | 🕐 $(date '+%H:%M')\n\n━━━━━━━━━━━━━━━━━━━━━\n📊 *今日数据*\n   今天: *${TODAY_COUNT}* 条\n   7天平均: ${WEEK_AVG} 条/天\n\n━━━━━━━━━━━━━━━━━━━━━\n📈 *最近3天*\n${RECENT_DAYS}\n\n━━━━━━━━━━━━━━━━━━━━━\n📊 *历史统计*\n   最高: ${MAX_COUNT} 条\n\n🌐 看板: https://tony1921.github.io/elon-twitter/dashboard.html\n\n━━━━━━━━━━━━━━━━━━━━━\n🤖 GitHub Actions Auto-update\",
              \"parse_mode\": \"Markdown\"
            }"
        if: success()
